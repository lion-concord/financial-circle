                    {activeTab === "club" && (
                      <ClubScreen
                        current={state.subscription}
                        onChoose={chooseTariff}
                        onOpenChat={() => setActiveTab("chat")}
                        onOpenWebinars={() => setActiveTab("webinars")}
                      />
                    )}
